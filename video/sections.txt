# Signal AI — section plan for the 3-minute YouTube demo
#
# Format: clip | chapter title | start sec | end sec | subtitle
# Leave start/end empty to use the whole clip.
#
# !! 檔名是假的 !! 下面五個 .mp4 是照講稿的五段命名的佔位名稱。
# 把它們改成你實際的檔名，或把你的檔案改成這些名字。
# 如果你的素材是「一整支連續錄影」而不是五個檔，改成這樣就好
# （同一個檔，用 start/end 切開）:
#   demo.mp4 | Signal in the background | 0   | 35  |
#   demo.mp4 | One decision             | 35  | 48  |
#   demo.mp4 | Where the info came from | 48  | 69  |
#   demo.mp4 | Who decides what         | 69  | 85  |
#   demo.mp4 | Ask and decide           | 85  | 113 |
#
# 先量長度再調秒數:
#   for f in *.mp4; do printf "%-26s %ss\n" "$f" "$(ffprobe -v error -show_entries format=duration -of csv=p=0 "$f" | cut -d. -f1)"; done
#
# 秒數是照講稿算的（130 字/分 + 每個畫面動作 0.8 秒），不是從素材量的。

background.mp4  | Signal in the background | 0 | 35 | Connect your tools, then forget it's there
decision.mp4    | One decision             | 0 | 13 | One thing actually needs you
evidence.mp4    | Where the info came from | 0 | 21 | Follow the claim back to its source
stakeholders.mp4| Who decides what         | 0 | 16 | Authority, not just opinion
ask.mp4         | Ask and decide           | 0 | 28 | It drafts, you approve, you decide
