# Signal AI — section plan for the 3-minute YouTube demo
#
# Format: clip | chapter title | start sec | end sec | subtitle
# Leave start/end empty to use the whole clip. When it's right, run:
#   bash ../.claude/skills/video-youtube/scripts/make-youtube.sh --sections sections.txt \
#     -o signal_demo_youtube.mp4 --title "Signal AI" --sub "The assistant that learns how you learn"
#
# The seconds below are a budget for fitting 3 minutes — they are NOT measured
# from your footage. Measure first, then correct them:
#   for f in *.mp4; do printf "%-22s %ss\n" "$f" "$(ffprobe -v error -show_entries format=duration -of csv=p=0 "$f" | cut -d. -f1)"; done
#
# Budget: 5 title cards x 2.5s = 12.5s, plus ~165s of footage = about 2:58.

onboarding.mp4 | Learns how you learn        | 0 | 30 | Learning Profile Onboarding
sequencing.mp4 | Follows your train of thought | 0 | 45 | Sequential Question Handling
format.mp4     | Your format, your call      | 0 | 28 | Format Customization
trust.mp4      | Every claim, checkable      | 0 | 32 | Trust Indicators & Verification
reflection.mp4 | It asks you back            | 0 | 30 | Critical Thinking & Reflection
